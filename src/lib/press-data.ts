import type { PressArticle } from '@/types';

/* ─────────────────────────────────────────────────────────
   Press / Media Coverage Data
   Replace sample entries with real articles as they are published.
   ───────────────────────────────────────────────────────── */

export const PRESS_ARTICLES: PressArticle[] = [
  // 실제 보도된 기사만 넣는다.
  // (예전에는 실존 언론사 이름을 붙인 예시 기사가 들어 있어 사이트에 그대로 노출됐다)
];
