// Font classes applied via CSS @font-face in globals.css
// These variables provide className strings for the font variables
export const pretendard = {
  variable: 'font-body-loaded',
};

export const syne = {
  variable: 'font-display-loaded',
};
